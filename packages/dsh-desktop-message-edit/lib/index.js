/** Host half of the DSH Desktop message-edit plugin. All behavior lives in
 * the browser client bundle; this entry only exists so the package can be
 * mounted as a normal Cordis loader row by the desktop profile. */
export function apply() {}
