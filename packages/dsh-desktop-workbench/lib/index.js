/** Host half of the DSH Desktop Workbench plugin. Native capabilities live in
 * the desktop main process and are reached through the preload bridge; this
 * entry only exists so the package can be mounted as a normal Cordis loader
 * row by the desktop profile. */
export function apply() {}
